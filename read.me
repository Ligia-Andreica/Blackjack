*** prerequisites
- download and install latest node.js

*** setup to run the app
- unzip the archive
- open cmd inside the Blackjack folder run the following commands:
- npm install (updates project dependencies)
- npm run build (builds the project)
- npm run live (starts the application on http://localhost:3000/)